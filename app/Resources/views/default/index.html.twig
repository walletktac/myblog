{% extends 'base.html.twig' %}

{% block body %}
    <h1>Blog</h1>
    
    {% for post in posts %}
        <h2>{{ post.title }}</h2>
        <p>{{ post.lead }}</p>
    {% endfor %}
    
    <div class="navigation">
        {{ knp_pagination_render(posts) }}
    </div>
    
    
{% endblock %}

